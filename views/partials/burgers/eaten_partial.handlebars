<li class="burger_eaten">
    {{burger_names}}
    <span>{{#if gone}}gone{{else}}here{{/if}}</span>
    <button type="button" class="delete_button btn btn-outline-secondary" data-id="{{id}}">Delete</button>
</li>